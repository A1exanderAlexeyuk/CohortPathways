% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BuildPlots.R
\name{createPathwaySankey}
\alias{createPathwaySankey}
\title{Create a Sankey Plot for Treatment Pathways}
\usage{
createPathwaySankey(
  cpResults,
  generationSet,
  maxPaths = 5,
  minCount = 10,
  colorPalette = NULL,
  showStepLabels = TRUE,
  showDropOff = FALSE,
  nodeWidth = 20,
  nodePadding = 15,
  fontSize = 12,
  plotTitle = "Cohort Pathway Sankey Diagram",
  plotHeight = 600,
  orientation = "h",
  linkOpacity = 0.4
)
}
\arguments{
\item{cpResults}{A list containing the results from CohortPathway analysis.
Must include \code{pathwaysAnalysisPathsData}, \code{isCombo}, and
\code{pathwayAnalysisCodesLong} data frames.}

\item{generationSet}{A data frame containing information about cohorts used for
mapping event codes to descriptive names. Must include \code{cohortId} and
\code{cohortName} columns.}

\item{maxPaths}{Integer. Maximum number of steps (depth) to include (default: 5).
Must be at least 2 to create meaningful links.}

\item{minCount}{Integer. Minimum count for a pathway to be included (default: 10).}

\item{colorPalette}{Character vector of hex colors for nodes. If \code{NULL} (default),
uses a built-in HCL palette. Colors are mapped by treatment name (same treatment =
same color across steps) and recycled if fewer colors than treatments.}

\item{showStepLabels}{Logical. Append step numbers to node labels (default: \code{TRUE}).}

\item{showDropOff}{Logical. Show "End" nodes for patients whose pathway terminates
before reaching the final step (default: \code{FALSE}).}

\item{nodeWidth}{Numeric. Node thickness in pixels (default: 20).}

\item{nodePadding}{Numeric. Vertical spacing between nodes in pixels (default: 15).}

\item{fontSize}{Numeric. Label font size (default: 12).}

\item{plotTitle}{Character. Plot title (default: "Cohort Pathway Sankey Diagram").}

\item{plotHeight}{Numeric. Plot height in pixels (default: 600).}

\item{orientation}{Character. \code{"h"} for horizontal or \code{"v"} for vertical
(default: \code{"h"}).}

\item{linkOpacity}{Numeric. Link transparency between 0 and 1 (default: 0.4).}
}
\value{
A plotly htmlwidget object containing the interactive Sankey diagram.
}
\description{
Creates an interactive Sankey diagram using plotly to visualize the flow of patients
through treatment pathways from CohortPathway analysis results. Each column of nodes
represents a step in the pathway, and the width of links represents the number of
patients transitioning between treatments.

The same treatment appearing at different steps is represented as distinct nodes,
with consistent coloring across steps for easy identification.
}
\examples{
\dontrun{
library(CohortPathways)

# Basic usage
sankeyPlot <- createPathwaySankey(
  cpResults = cpResults,
  generationSet = generationSet,
  maxPaths = 5,
  minCount = 10
)
sankeyPlot

# Customize appearance
createPathwaySankey(
  cpResults = cpResults,
  generationSet = generationSet,
  maxPaths = 3,
  minCount = 5,
  colorPalette = c("#1b9e77", "#d95f02", "#7570b3", "#e7298a"),
  showDropOff = TRUE,
  linkOpacity = 0.3,
  fontSize = 14,
  plotTitle = "Treatment Pathways"
)
}
}
